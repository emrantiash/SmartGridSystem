import styleSheet from '../style' ;

const styles = {

    button : {
        // marginTop : 10 ,
        fontSize : styleSheet.normSize,
        letterSpacing : styleSheet.letterspacing,
        // color : commonStyle.color.white,
        fontFamily : styleSheet.formTile,
        fontWight : 'bold',
        borderRadius :10,
        // marginTop  : 15 
    }
}

export default styles 